# Content Delivery Network (CDN) Simulator
A mini-CDN that replicates static assets across “edge” servers. Users upload assets to an origin server; multiple edge servers cache and serve them based on simulated geographic requests
